import React from 'react';
const providers=[['WATCHLIST A','SANCTIONS'],['WATCHLIST B','PEP'],['WATCHLIST C','ADVERSE MEDIA'],['WATCHLIST D','CUSTOM LIST']];
export default function ScreeningDemo(){const [run,setRun]=React.useState(false);const [done,setDone]=React.useState(false);const start=()=>{setRun(true);setDone(false);setTimeout(()=>setDone(true),1800)};return <section className="screening-demo" id="screening-demo"><div className="container"><div className="demo-top"><div><p className="eyebrow">PLATFORM CAPABILITY</p><h2>Parallel <span>Screening</span></h2><p className="demo-intro">One subject. Multiple screening sources. A single auditable decision path.</p></div><button className="button primary" onClick={start} disabled={run&&!done}>{done?'Run again':'Run screening'} <span>→</span></button></div><div className="screening-shell"><div className="subject"><small>SUBJECT</small><strong>ALEXANDER MORGAN</strong><span>Travel document · P•••••••842</span></div><div className="provider-grid">{providers.map(([name,type],i)=><div className={'provider '+(run?'running ':'')+(done?'clear':'')} style={{'--delay':`${i*180}ms`}} key={name}><div className="provider-icon">{done?'✓':String(i+1).padStart(2,'0')}</div><div><b>{name}</b><small>{type}</small></div><span className="provider-status">{done?'CLEAR':run?'SCREENING':'READY'}</span></div>)}</div><div className={'decision '+(done?'visible':'')}><div><small>CONSOLIDATED RESULT</small><strong>CLEAR</strong></div><span>All configured screening sources returned no match.</span></div></div><p className="demo-disclaimer">Demonstration only. Provider names, results and data are simulated.</p></div></section>}
