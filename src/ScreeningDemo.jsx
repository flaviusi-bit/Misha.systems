import React from 'react';
const providers=[['WATCHLIST A','SANCTIONS'],['WATCHLIST B','PEP'],['WATCHLIST C','ADVERSE MEDIA'],['WATCHLIST D','CUSTOM LIST']];
export default function ScreeningDemo(){const [run,setRun]=React.useState(false);const [done,setDone]=React.useState(false);const start=()=>{setRun(true);setDone(false);setTimeout(()=>{setDone(true);setRun(false)},1800)};return <section className="screening-demo" id="screening-demo"><div className="container"><div className="demo-top"><div><p className="eyebrow">eTA CAPABILITY · PARALLEL SCREENING</p><h2>Screen once. <span>Check everywhere.</span></h2><p className="demo-intro">During the eTA journey, the applicant can be screened against multiple configured watchlists in parallel. Results are consolidated into a single decision path.</p></div><button className="button primary" onClick={start} disabled={run}>{run?'Screening…':done?'Run again':'Run screening'} <span>→</span></button></div><div className="screening-shell"><div className="subject"><small>eTA APPLICANT</small><strong>ALEX MORGAN</strong><span>Travel document · P•••••••842</span></div><div className="provider-grid">{providers.map(([name,type],i)=><div className={'provider '+(run?'running ':'')+(done?'clear':'')} style={{'--delay':`${i*180}ms`}} key={name}><div className="provider-icon">{done?'✓':String(i+1).padStart(2,'0')}</div><div><b>{name}</b><small>{type}</small></div><span className="provider-status">{done?'CLEAR':run?'SCREENING':'READY'}</span></div>)}</div><div className={'decision '+(done?'visible':'')}><div><small>eTA SCREENING RESULT</small><strong>CLEAR</strong></div><span>Configured screening sources returned no match in this simulated journey.</span></div></div><p className="demo-disclaimer">Demonstration only. Watchlists, provider names, results and data are simulated.</p></div></section>}
